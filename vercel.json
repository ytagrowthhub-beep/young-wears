{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -f next.config.mjs ]; then npm ci; else cd frontend && npm ci; fi",
  "buildCommand": "if [ -f next.config.mjs ]; then npm run build; else cd frontend && npm run build; fi"
}
