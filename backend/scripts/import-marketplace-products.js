/**
 * Previously imported marketplace rows into MongoDB. The API now uses an in-memory catalog only.
 * Extend products by editing seed data in backend/server.js or move catalog to Supabase Postgres.
 */
console.error("[import:products] MongoDB was removed from this project; import is disabled.");
process.exit(1);
